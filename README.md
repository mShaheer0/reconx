# Recon Orchestrator (skeleton)

Phase-1 skeleton for the Recon Orchestrator project. This repo implements the CLI parsing and placeholders for later phases.

WARNING: Only run active scans against targets you own or have written permission to test.

Usage (after installing requirements):

```bash
python main.py example.com
python main.py example.com --active
```
